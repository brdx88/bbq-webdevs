<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <meta charset="utf-8">
        <title></title>
    </head>
    <body>

        <% if (nameOfDay === "Saturday" || nameOfDay === "Sunday") { %>
            <h1 style="color: purple">It's <%= nameOfDay %> !</h1>
        <% } else { %>
            <h1 style="color: blue">It's <%= nameOfDay %> !</h1>
        <% } %>

    </body>
</html>
